name: schedule-test
on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Print time
        run: |
          date -u
          echo "scheduled run"
